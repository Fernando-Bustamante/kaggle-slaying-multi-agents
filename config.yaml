# DEBUG ONLY — not used in autonomous mode.
# When running `python main.py <competition>`, this file is ignored.
# Only loaded when no competition name is provided (manual override for local testing).

competition:
  name:
  id_column:
  target_column:
  task_type:
  metric:
  goal:
  predict_type:

model:
  algorithms:
  n_trials:
  cv_folds:
  sample_size:

output:
  data_dir:
  models_dir:
  submission_file:
